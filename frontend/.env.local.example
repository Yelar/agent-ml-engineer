# Backend Configuration
# Set to "true" to use your backend instead of Vercel Gateway
NEXT_PUBLIC_USE_BACKEND=true
NEXT_PUBLIC_BACKEND_URL=http://localhost:8000

# Database (Required for auth & chat history)
POSTGRES_URL=your-postgres-connection-string

# Auth Configuration (Required for authentication)
AUTH_SECRET=your-secret-key-here-generate-with-openssl-rand-base64-32

# Optional: Vercel Gateway (only if not using backend)
# GATEWAY_URL=https://gateway.ai.cloudflare.com/v1/your-account/your-gateway/openai
